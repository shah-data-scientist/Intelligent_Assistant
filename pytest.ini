[pytest]
markers =
    integration: integration tests that require real data or APIs
    performance: performance tests that measure latency or throughput
    evaluation: evaluation framework tests (metrics, datasets, evaluators)
    slow: slow-running tests (typically evaluation end-to-end tests)
